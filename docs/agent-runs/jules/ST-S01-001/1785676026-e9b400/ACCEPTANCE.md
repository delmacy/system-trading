# Acceptance Criteria

- npm ci, lint, typecheck, tests and build pass from a clean checkout.
- trading-protocols exposes and validates the canonical event envelope.
- CI runs the same mandatory gates and no future capability is represented as implemented.
