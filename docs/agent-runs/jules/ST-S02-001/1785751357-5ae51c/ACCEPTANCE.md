# Acceptance Criteria

- PatternDefinition is a public runtime-validated contract matching the documented minimum fields and lifecycle.
- npm ci, lint, typecheck, tests and build pass without weakening existing gates.
- Only the declared System Trading files change; no Builder contract, persistence, UI or generic registry is introduced.
